You are working on an existing web application that uses Three.js to render interactive 3D network layers and nodes.

Use both threejs-skills and threejs-postprocessing to improve the frontend UI/UX and Three.js experience, while preserving existing product behavior.
Also use the token-saviour skill. 1 feature - 1 commit (no push).

Primary objective:
Refine the application into a modern, polished, premium-feeling interactive 3D experience without breaking backend functionality, API contracts, routing, data loading, or existing user flows.

Non-negotiable constraints:
- Do not change backend behavior, API endpoints, request shapes, response shapes, authentication behavior, or data contracts.
- Do not replace real data with mock data.
- Do not perform a wholesale rewrite unless the existing structure is clearly broken and the rewrite is smaller, safer, and justified.
- Preserve the existing purpose of the app: interactive exploration of 3D network layers and nodes.
- Prioritize usability, readability, performance, and stability over visual effects.
- Any visual enhancement must degrade gracefully on lower-end devices.
- Avoid effects that obscure nodes, labels, edges, controls, or important UI information.
- Respect accessibility basics: keyboard usability where applicable, sufficient contrast, reduced-motion handling, and readable typography.

Before making changes:
1. Inspect the project structure, frontend framework, Three.js scene architecture, data flow, API usage, and available scripts.
2. Identify the key user flows and routes.
3. Run the app and note any existing console errors, rendering problems, layout issues, and obvious performance problems.
4. Establish a baseline using Playwright where possible: app loads, main scene renders, nodes/layers are visible, core interactions work, and no critical console errors occur.
5. Create a concise implementation plan listing the main files/components likely to change.

Execution strategy:

Phase 1 — Stabilization and Three.js correctness
Use threejs-skills to audit and fix:
- Scene setup, renderer configuration, camera setup, controls, resize handling, and render loop structure.
- Asset/data loading behavior.
- Raycasting/picking/interactions.
- Object lifecycle management, including cleanup/disposal of geometries, materials, textures, render targets, and event listeners.
- Runtime errors, memory leaks, broken rendering, unstable interactions, or incorrect scene scaling.
- DPR handling and canvas resizing.

Validation after Phase 1:
- Run the relevant build/lint/test commands discovered in the repo.
- Use playwright-cli to verify that the app loads, the scene renders, and key interactions still work.
- Check browser console for errors and warnings.
- Fix issues before proceeding.

Phase 2 — UX and interaction refinement
Improve the 3D interaction experience:
- Camera behavior, zoom limits, target framing, damping, reset behavior, and navigation feel.
- Lighting setup and scene readability.
- Hover, selection, focus, and active-node/layer states.
- Responsiveness across desktop, tablet, and mobile sizes.
- Smoothness of transitions and interaction feedback.
- Node/layer readability at different zoom levels.
- Avoid disorienting motion or excessive animation.

Performance expectations:
- Avoid unnecessary per-frame allocations.
- Avoid expensive raycasting when not needed; throttle or optimize pointer interactions if necessary.
- Use instancing, batching, level-of-detail, visibility culling, or simplified materials where appropriate.
- Cap device pixel ratio where needed for stable performance.
- Keep the render loop efficient and pause/reduce work when the scene is idle if appropriate.

Validation after Phase 2:
- Use playwright-cli to test primary interaction flows.
- Confirm responsive behavior at common viewport sizes.
- Verify no new console errors.
- Check that interaction changes do not break existing behavior.

Phase 3 — Visual enhancement with postprocessing
Use threejs-postprocessing only after the scene is stable.

Apply post-processing effects sparingly and intentionally:
- Bloom only for meaningful highlights or emissive accents.
- Tone mapping and exposure for balanced visual quality.
- Depth of field only if it improves focus and does not hurt usability.
- Color grading only if it improves cohesion and readability.
- Avoid over-blooming, excessive blur, crushed contrast, washed-out colors, or visual noise.

Implementation requirements:
- Keep postprocessing configurable and easy to disable.
- Provide graceful fallback for lower-end devices or reduced-motion/performance-sensitive contexts.
- Ensure UI overlays, labels, controls, and important scene elements remain readable.
- Do not introduce major GPU cost without a clear visual benefit.

Validation after Phase 3:
- Use playwright-cli to verify rendering still works.
- Check for WebGL/runtime errors.
- Compare before/after visual clarity.
- Confirm postprocessing does not degrade interaction responsiveness or readability.

Phase 4 — Non-3D frontend UI/UX polish
Improve the surrounding frontend experience:
- Layout, spacing, typography, hierarchy, panels, controls, buttons, legends, menus, loading states, empty states, and error states.
- Responsive behavior across screen sizes.
- Transitions and microinteractions, keeping them subtle and purposeful.
- Cohesive design system across UI panels and the Three.js canvas.
- Visual alignment between the 2D interface and the 3D scene.

Do not:
- Introduce unnecessary UI complexity.
- Hide important controls.
- Change product semantics.
- Break existing routes or API-driven states.

Validation after Phase 4:
- Use playwright-cli to test key user flows.
- Confirm layout works at desktop, tablet, and mobile sizes.
- Check keyboard focus, contrast, and reduced-motion behavior where applicable.
- Confirm no backend/API behavior changed.

Phase 5 — Final validation and regression pass
Run a final validation loop:
- Install/build/lint/test commands available in the project.
- Playwright checks for main routes and key flows.
- Console error checks.
- Visual smoke test of the Three.js scene.
- Interaction smoke test: camera controls, node hover, node selection, layer interaction, resize, and reload.
- Performance sanity check for obvious frame drops, memory leaks, runaway render loops, or excessive GPU effects.

Iterate until:
- No functional regressions exist.
- No critical console/runtime errors exist.
- The Three.js scene is stable, readable, performant, and visually polished.
- The UI feels cohesive, modern, and intentional.
- Backend functionality and API contracts remain unchanged.
- Postprocessing improves the experience without compromising usability.

Final response should include (write this to a REPORT_THREEJS.md file):
- Summary of major improvements.
- Files changed.
- Validation commands run.
- Playwright flows tested.
- Any remaining risks, tradeoffs, or follow-up recommendations.